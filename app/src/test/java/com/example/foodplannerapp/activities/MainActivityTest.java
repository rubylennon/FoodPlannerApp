package com.example.foodplannerapp.activities;

import org.junit.jupiter.api.Test;

public class MainActivityTest {

    @Test
    void onCreate() {
    }

    @Test
    void onRecipeClick() {
//        mockStatic(ReportFragment.class);
//        MainActivity activity = spy(new MainActivity());
//        activity.onRecipeClick(0);
//        doNothing().when(activity).setContentView(R.layout.activity_main);
//        doReturn(mock(BaseMenuActivity.class)).when(activity).getDelegate();
//
//        // Call the method
//        activity.onCreate(null);
//
//        // Verify that it worked
//        verify(activity, times(1)).setContentView(R.layout.activity_main);
    }

}